name: tests for lifebit-ai/simulate
# This workflow is triggered on pushes and PRs to the repository.
on: [push, pull_request]

jobs:
  basic_test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nxf_ver: ['20.01.0', '']
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          export NXF_VER=${{ matrix.nxf_ver }}
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile - simulate plink, vcf and gwas summary statistics
        run: |
          nextflow run ${GITHUB_WORKSPACE} -profile test
  various_flags_test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        gwas_pheno_trait_type: ['binary', 'quantitative']
        effective_population_size: ['11418','11000']
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile and various optional flags 
        run: |
          nextflow run ${GITHUB_WORKSPACE} --gwas_pheno_trait_type ${{ matrix.gwas_pheno_trait_type }} --effective_population_size ${{ matrix.effective_population_size }} -profile test
  test_sim_cb_output_query:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile and various optional flags 
        run: |
          nextflow run ${GITHUB_WORKSPACE} -profile test_sim_cb_output_query
  test_sim_cb_output_yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile and various optional flags 
        run: |
          nextflow run ${GITHUB_WORKSPACE} -profile test_sim_cb_output_yaml
  test_sim_cb_output_plink_only:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile and various optional flags 
        run: |
          nextflow run ${GITHUB_WORKSPACE} -profile test_sim_cb_output_plink_only
    test_sim_cb_output_vcf_only:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
      - name: Run pipeline with basic test profile and various optional flags 
        run: |
          nextflow run ${GITHUB_WORKSPACE} -profile test_sim_cb_output_vcf_only